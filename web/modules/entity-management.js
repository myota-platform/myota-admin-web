// Entity management is implemented by the shared Leaflet workspace. This
// entry point is kept separate so the navigation surface has an explicit page
// module and can later grow management-only workflows without changing review.
